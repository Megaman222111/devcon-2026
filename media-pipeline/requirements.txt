# PyTorch — install separately with CUDA 11.8 wheel (see setup.sh)
# pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 \
#   --index-url https://download.pytorch.org/whl/cu118

# API server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.5.0

# HuggingFace model hub and inference utilities
huggingface_hub>=0.20.3
transformers>=4.37.0
accelerate>=0.26.0

# Video I/O (fallback save if wan.utils.utils.cache_video unavailable)
imageio>=2.33.0
imageio-ffmpeg>=0.4.9

# AWS S3 / EC2 metadata
boto3>=1.34.0

# General utilities
numpy>=1.24.0
tqdm>=4.66.0
